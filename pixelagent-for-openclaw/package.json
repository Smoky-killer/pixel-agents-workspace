{
  "name": "pixelagent-for-openclaw",
  "version": "1.0.0",
  "description": "Pixel art office visualization for OpenClaw agents",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"node server/index.js\" \"cd webview-ui && npm run dev\"",
    "build": "cd webview-ui && npm run build",
    "build:watch": "cd webview-ui && vite build --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pngjs": "^7.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
